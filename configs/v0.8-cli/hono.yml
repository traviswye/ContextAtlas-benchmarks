# ContextAtlas config for hono benchmark target — v0.8-cli variant.
#
# Step 1.1.b.0 (Q1.1.D.4-8 lock). Factorial-design CLI atlas-
# extraction substrate per v0.8 sandbox structure. Paths route to
# `v0.8/` sub-paths; the canonical `configs/hono.yml` substrate is
# preserved bit-exactly (default CA_BENCHMARK_VARIANT-unset
# behavior). See `configs/hono.yml` for path-resolution rationale.

version: 1

languages: [typescript]

source:
  root: v0.8/repos/hono/

adrs:
  path: v0.8/adrs/hono/

docs:
  include: []

atlas:
  committed: true
  path: v0.8/atlases-cli/hono/atlas.json
  local_cache: v0.8/atlases-cli/hono/index.db
